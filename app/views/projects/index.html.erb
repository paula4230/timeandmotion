<%= link_to 'Add Project', new_category_project_path(@category, @project), class:"add-category"%>
<% @projects.each do |p| %>
    <%= p.id %>
    <%= p.RxC %>
    <%= p.state %>
    <%= p.workflow_date %>
    <%= p.ticket_level %>
    <%= p.notes %>

<% end %>