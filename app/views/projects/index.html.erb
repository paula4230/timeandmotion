<%= link_to 'Add Project', new_project_path%>
<% @projects.each do |p| %>
    <%= p.id %>
    <%= p.RxC %>
    <%= p.state %>
    <%= p.workflow_date %>
    <%= p.ticket_level %>
    <%= p.notes %>

<% end %>