<div>
<%= link_to 'Add Category', new_category_path %>
  <table>
  <tbody>
    <% @categories.each do |category| %>
    <tr>
        <td>
            <%= link_to category.intent, category_steps_path(category.id)%>
            <%= category.status%>
            <%= category.due%>
        </td>
    </tr>
    <% end %>
  </tbody>
  </table>
</div>